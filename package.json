{
  "name": "global-trade-site-framework",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable foreign-trade showcase site framework with Supabase, EdgeOne Pages, and WordPress/WooCommerce migration tools.",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @global-trade/site dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
